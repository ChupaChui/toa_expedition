# Expedition Tracker

Small Flask website for tracking character or expedition info such as:

- name
- STR mod
- max slots
- used slots
- note

## Run

```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

Open `http://127.0.0.1:5000`.
